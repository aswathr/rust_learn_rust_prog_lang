fn main() {
    // a obvious function signature and definition
    println!("Hello world!"); // The only other thing to remember now is that `println!` is a macro and not a regular function call because of the `!`
}
